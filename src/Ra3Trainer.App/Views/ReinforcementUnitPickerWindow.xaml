<Window x:Class="Ra3Trainer.App.Views.ReinforcementUnitPickerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="选择增援单位"
        Height="640"
        Width="760"
        MinHeight="480"
        MinWidth="620"
        WindowStartupLocation="CenterOwner"
        Background="#F4F5F7">
  <DockPanel Margin="16">
    <StackPanel DockPanel.Dock="Top">
      <Grid Margin="0,0,0,10">
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="*" />
          <ColumnDefinition Width="Auto" />
          <ColumnDefinition Width="Auto" />
          <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>
        <TextBox Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                 Padding="8,6"
                 VerticalContentAlignment="Center"
                 Margin="0,0,10,0" />
        <Button Grid.Column="1"
                Content="导入文件"
                Command="{Binding ImportCommand}"
                Padding="12,7"
                Margin="0,0,8,0" />
        <Button Grid.Column="2"
                Content="确认"
                Command="{Binding ConfirmCommand}"
                Padding="12,7"
                Margin="0,0,8,0" />
        <Button Grid.Column="3"
                Content="取消"
                Command="{Binding CancelCommand}"
                Padding="12,7" />
      </Grid>
      <TextBlock Text="{Binding StatusMessage}" Foreground="#4B5563" Margin="0,0,0,4" />
    </StackPanel>

    <ListView ItemsSource="{Binding FilteredUnits}"
              SelectedItem="{Binding SelectedUnit, Mode=TwoWay}"
              BorderBrush="#D7DAE0"
              BorderThickness="1"
              Background="White">
      <ListView.View>
        <GridView>
          <GridViewColumn Header="代码" Width="150" DisplayMemberBinding="{Binding CodeText}" />
          <GridViewColumn Header="名称" Width="500" DisplayMemberBinding="{Binding Name}" />
        </GridView>
      </ListView.View>
    </ListView>
  </DockPanel>
</Window>
